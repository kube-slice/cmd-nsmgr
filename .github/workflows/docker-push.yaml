---
name: Docker push
on:
  push:
    branches:
      - main
  workflow_run:
    types:
      - completed
    workflows:
      - 'automerge'
jobs:
  push:
    if: ${{ github.repository != 'networkservicemesh/cmd-template' && (github.event.workflow_run.conclusion == 'success' && github.actor == 'nsmbot' || github.event_name == 'push') }}
    uses: networkservicemesh/.github/.github/workflows/docker-push.yaml@fe8cd964c2e13a408077d9a78b939934e3b4521f # main
    secrets:
      docker-login: ${{ secrets.DOCKER_LOGIN }}
      docker-password: ${{ secrets.DOCKER_PASSWORD }}
